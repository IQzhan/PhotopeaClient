---
description: 版本与 GitHub Release 流程（手动发版；口头「更新版本然后推 release」时自动执行）
alwaysApply: true
---

# 版本 / Release

## 原则

- **打包 ≠ 发版**：`npm run pack` 可随时做（含测试），不自动发 Release、不自动改版本。
- **改版本号很少做**：只有用户明确说要更新版本时才改 `package.json` 的 `version`。
- **发 Release 也是手动**：用户明确说推 release / 发版时才执行。
- 日常改动写入 `CHANGELOG.md` 的 `## [Unreleased]`。

## 推荐发版路径（稳定）

推送 tag → `.github/workflows/release.yml` 在 GitHub 上 `npm run pack` 并创建 Release（避免本机访问 `api.github.com` 超时）。

用户下载：https://github.com/IQzhan/PhotopeaClient/releases/latest

## 用户口令 → Agent 动作

### 「更新版本然后推 release」（或同义）

默认 **patch** 递增（除非用户指定 minor/major），自动做完：

1. 读 `package.json` 与 `CHANGELOG.md` `[Unreleased]`，拟定下一版本与说明。
2. 把 Unreleased 挪到 `## [x.y.z] - YYYY-MM-DD`，保留空的 Unreleased 占位。
3. 更新 `package.json` 的 `version`。
4. 提交并 `git push origin main`。
5. `git tag vX.Y.Z` && `git push origin vX.Y.Z`（触发 Actions 发版）。
6. 如需本机验证可再 `npm run pack`；不要默认依赖本机 `gh release create`。
7. 回复用户 latest / tag 链接；可用 `gh run list` 查看 Actions 是否成功。

### 「推 release」（不升版本）

1. 确认 CHANGELOG 已有当前 `package.json` 版本小节。
2. 确保该 `vX.Y.Z` tag 已推送；若无则打 tag 并 push（触发 Actions）。
3. 若 tag 已在远程但 Release 缺失：`gh workflow run Release -f tag=vX.Y.Z` 补发。
4. 回复链接。

### 仅「打包」

只跑 `npm run pack`，不改版本、不发 Release。

## 命令

| 命令 | 作用 |
|------|------|
| `npm run pack` | 只打包 |
| `npm run release` | 校验 CHANGELOG 并确保 tag 已推送（Actions 发版） |
| `npm run release -- --local --pack` | 本机打包并用 gh 上传（网络不稳时可能失败） |
| `node scripts/changelog-notes.js 1.0.0` | 只打印某版本的 Release 正文 |
