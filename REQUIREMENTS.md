# PhotopeaClient 需求说明

本文汇总从首次对话到当前的全部需求，去重润色，不删减、不弱化。后续新增需求应追加到文末「变更记录」。

## 1. 产品定位

本程序是 **Photopea 官方网站的桌面客户端壳**，不是二次修改版编辑器。

- 展示地址固定为线上 [https://www.photopea.com/](https://www.photopea.com/)。
- **运行时磁盘缓存**（`userData/pp-cache`）：访问 `photopea.com` / `vecpea.com` 子资源时在线后台预热落盘；离线时重定向到 `pp-asset` 回落缓存；主文档仍走 HTTPS，失败则尝试缓存壳页。安装包不预置整站镜像，首次需联网一次。
- 不实现自有编辑能力；页面内编辑器即全部功能。
- 实现保持最简：双击可执行文件即可使用。
- 账户 / 云同步等依赖线上接口的功能在离线时不可用（本地文件编辑不受影响）。

## 2. 窗口与外观

- 无浏览器外框、无标签栏、无地址栏、无系统标题栏。视觉外框就是 Photopea 编辑器自身界面。
- 窗口可按边框正常缩放（最小约 800×560）。
- 启动尺寸约 1280×800。
- 程序图标使用 **Photopea 官方高清图标**（透明背景 PNG，来源官方 `promo/icon512.png` / `logo.svg`）。

## 3. 顶栏交互（拖动 / 按钮 / 遮罩）

原先用一整块高 z-index 不透明遮罩盖住「更多」右侧整条顶栏，既当拖动手柄又当遮挡，导致：

- 遮罩高度超出顶栏，点开「更多」时下拉列表左上角被切掉（缺角）；
- 「账户」等按钮偶尔从遮罩缝里露出来。

正确行为：

- **不整条覆盖顶栏**，也不用不透明遮挡块压住 Photopea 内部弹窗（如 Plugins）。
- **拖动**：仅在右上角控件左侧留一小段透明拖动区；内部大面板打开时自动隐藏拖动区与右侧遮挡条，避免挡住面板顶栏。
- **隐藏**：通过 DOM/样式隐藏网页全屏四角图标、右上角「关于 / 反馈错误 / 学习 / Blog / API」及社交链接；**保留账户入口**，但把刺眼红色改成与顶栏协调的灰。
- **窗口控件**（设置、最小化、最大化、关闭）放在网页右上角；设置齿轮用青绿强调色。
- **内置浮层弹窗**（Plugins 资源库、新建/导出/首选项等）仍留在主页面内，与编辑器天然联动；打开时壳层让位、放开祖先 overflow，必要时临时扩大主窗口以免被边缘裁切。不要把图层面板等停靠窗当成这类弹窗。
- 最大化是 **系统窗口最大化**，不覆盖任务栏；禁止网页 Fullscreen；F11 与页面内全屏请求都改为最大化/还原。
- 「更多」等菜单与内部弹窗完整显示，不得缺角、不得被壳层压住。

## 4. 广告与启动页

- 去掉右侧广告栏及广告本身（含预留空列），编辑器占满窗口。
- 尽量跳过营销/介绍首页，直接进入编辑器（`intro: false`）。**不要**自动新建 Untitled 空白文档。

## 5. 打开本地文件

- 双击已绑定后缀的文件，用本程序打开并载入 Photopea。
- 冷启动双击时，等编辑器就绪后打开该文件本身，而不是先开 Untitled。
- 打开后的文档标题使用原文件名。
- 从磁盘读取/载入文件期间显示加载遮罩（文件名 + 转圈），**直到文档真正出现在编辑器中**再关闭；失败须显示明确错误，不得无声假死。
- 本地文件经专用协议按路径读取（避免大文件整包 IPC），再交给 Photopea 内部 file input / drop 路径。
- 默认绑定 **`.psd`**。
- 提供设置弹窗：列出 Photopea 官方支持的全部可打开格式，勾选后写入当前用户文件关联；取消勾选则解除本程序关联。
- 已运行时再双击文件，应在现有窗口中打开，而不是再开一个实例。

## 6. 设置弹窗

- 风格与编辑器深色顶栏一致（深灰底、青绿强调色 `#18a497`）。
- 可全选 / 全不选 / 恢复默认（仅 PSD）/ 保存并绑定。
- 从主窗口右上角齿轮进入。

## 7. 分发

- 提供一键打包脚本：`pack.bat`（Windows）与 `pack.sh`。
- 产物为可独立运行的 exe 及完整依赖目录（`dist/win-unpacked`），并打 zip。
- **分发给他人时不需要 Node.js 或本仓库开发环境**。打包机需要 Node.js。

## 8. 明确不做

- 不预打包 / 不镜像整站进安装包；缓存仅运行时按需写入用户目录。
- 不篡改 Photopea 编辑功能本身。
- 不以网页 Fullscreen 作为「全屏」。

## 变更记录

- 2026-09-07 初稿：壳、无框、拖动、缩放、双击 exe、PSD 关联、始终在线最新。
- 2026-09-07：去广告栏、去右上角链接、去账户按钮、跳过营销页、最大化不盖任务栏。
- 2026-09-07：修复顶栏遮罩缺角与账户闪现；官方透明图标；全格式绑定设置；一键打包；本文档。
- 2026-09-07：打包产物为 `dist/win-unpacked` 文件夹（exe + 依赖 + `associations.json`）。启动时按当前 exe 绝对路径写入当前用户文件关联；设置勾选的格式双击应打开本编辑器。
- 2026-09-07：不再自动新建 Untitled；冷启动双击打开目标文件本身；打开时显示加载提示并把文档标题设为原文件名。
- 2026-09-07：本地文件改为走 Photopea 内部同一路径（隐藏 file input + FileReader），保留原文件名，避免 data URI/base64 导致的 `file.psd` 与超长等待。
- 2026-09-09：启动不再同步重写全部文件关联（路径未变则跳过，改动在后台进行）；设置齿轮加大并改为青绿强调色；设置窗改用 flex 布局并按内容尺寸显示，避免底栏按钮被裁切。
- 2026-09-09：去掉顶栏不透明遮挡块与过高 z-index，避免挡住 Plugins 等内部弹窗；账户/关于等改为 DOM 隐藏；拖动区缩小，面板打开时自动让路。
- 2026-09-09：去掉顶栏四角全屏图标；恢复账户按钮并改成协调灰；Plugins 等浮层仍内置，壳层让位并可临时扩窗，避免被裁切（区分于编辑器停靠窗）。
- 2026-09-11：增加 photopea.com / vecpea.com 运行时磁盘缓存（在线后台预热；离线 pp-asset 回落；主文档 HTTPS + 失败回落缓存壳）；本地文件改走 `pp-file` 协议；打开遮罩对齐画布/文档就绪，失败可见提示。
